{
  "name": "tafutakeja",
  "version": "1.0.0",
  "description": "TafutaKeja — a Kenyan property marketplace built with the MERN stack",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n api,web -c teal,yellow \"npm run server\" \"npm run client\"",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "seed": "npm run seed --prefix backend",
    "seed:demo": "npm run seed:demo --prefix backend",
    "ingest": "npm run ingest --prefix backend",
    "images:optimize": "npm run images:optimize --prefix backend",
    "test": "npm test --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
